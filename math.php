<?php include 'header.php'; ?>
<?php include 'sidebar.php'; ?>
<div class="content">
    <header>
        <h1>PHP Math</h1>
    </header>
    <main>
        <section class="section" id="math">
            <h2>PHP Math</h2>
            <p>PHP provides many built-in math functions, such as <code>abs()</code>, <code>pow()</code>, <code>sqrt()</code>, <code>round()</code>, etc.</p>
            <pre>&lt;?php
    echo abs(-5);      // 5
    echo pow(2, 3);    // 8
    echo sqrt(16);     // 4
    echo round(3.7);   // 4
?&gt;</pre>
            <p class="bangla">PHP-তে অনেক বিল্ট-ইন ম্যাথ ফাংশন আছে, যেমন <code>abs()</code>, <code>pow()</code>, <code>sqrt()</code>, <code>round()</code> ইত্যাদি।</p>
            <pre class="bangla">&lt;?php
    echo abs(-৫);      // ৫
    echo pow(২, ৩);    // ৮
    echo sqrt(১৬);     // ৪
    echo round(৩.৭);   // ৪
?&gt;</pre>
        </section>
    </main>
<?php include 'footer.php'; ?> 